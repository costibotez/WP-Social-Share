<?xml version="1.0"?>
<ruleset name="WP Social Share">
	<description>Coding standard for the WP Social Share plugin.</description>

	<file>toptal-social-share.php</file>
	<file>uninstall.php</file>
	<file>includes</file>

	<arg value="sp"/>
	<arg name="extensions" value="php"/>
	<arg name="parallel" value="4"/>

	<config name="minimum_wp_version" value="5.0"/>
	<config name="testVersion" value="7.4-"/>

	<rule ref="WordPress-Extra">
		<!-- The plugin predates these rules; prefixes are toptal_ss/TopTal_SS. -->
		<exclude name="WordPress.Files.FileName"/>
		<!-- Short array syntax and modern formatting are fine. -->
		<exclude name="Universal.Arrays.DisallowShortArraySyntax"/>
	</rule>

	<rule ref="PHPCompatibilityWP"/>

	<rule ref="WordPress.WP.I18n">
		<properties>
			<property name="text_domain" type="array">
				<element value="toptal-ss"/>
			</property>
		</properties>
	</rule>

	<rule ref="WordPress.NamingConventions.PrefixAllGlobals">
		<properties>
			<property name="prefixes" type="array">
				<element value="toptal_ss"/>
				<element value="TopTal_SS"/>
				<element value="TOPTAL_SS"/>
			</property>
		</properties>
	</rule>

	<!-- The smoke tests stub WordPress core functions by design; they are
	     exercised by `composer test`, not held to WPCS. -->
	<exclude-pattern>tests/*</exclude-pattern>
	<exclude-pattern>vendor/*</exclude-pattern>
</ruleset>
